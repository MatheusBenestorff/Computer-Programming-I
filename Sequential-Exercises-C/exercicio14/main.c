#include <stdio.h>

void main()
{
    float base, altura;

    printf("Digite o valor da base e da altura, respectivamente: ");
    scanf("%f" "%f", &base, &altura);

    printf("A area do triangulo: %.2f", (base * altura)/2);
}
